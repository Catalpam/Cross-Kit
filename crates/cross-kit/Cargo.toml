[package]
name = "cross-kit"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
cross-kit-core = { path = "../cross-kit-core" }
cross-kit-macros = { path = "../cross-kit-macros" }

[dev-dependencies]
serde_json = "1.0"

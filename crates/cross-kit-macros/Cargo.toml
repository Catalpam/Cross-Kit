[package]
name = "cross-kit-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
cross-kit-core = { path = "../cross-kit-core" }
proc-macro2 = "1.0"
quote = "1.0"
serde_json = "1.0"
syn = { version = "2.0", features = ["full"] }
